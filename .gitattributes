# Line-ending policy for this repository.
#
# Declared here so the convention belongs to the project and not to each
# machine core.autocrlf setting. Without this file, a clone on Linux, a CI
# runner, or an agent sandbox can disagree with a Windows checkout about
# what counts as a change.

# Default: treat files as text, store them as LF in the repository, and
# check them out as LF on every platform.
* text=auto eol=lf

# Binary assets: never convert line endings, never diff as text.
*.woff2 binary
*.woff  binary
*.ttf   binary
*.otf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.gif   binary
*.ico   binary
*.pdf   binary
*.docx  binary
*.xlsx  binary
*.pptx  binary
*.ogg   binary
*.mp3   binary
*.mp4   binary
*.webm  binary
*.zip   binary

# Windows-only scripts keep CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
